# SteelBuild Pro — Session Handoff (2026-06-20)

Picks up a long security/quality session driven by a multi-model **audit (findings #1–#22)**
plus a prior **code review (criticals C1–C4, I1–I5)**. Working method: verify each finding
against *current* code (several were stale), fix the real ones, validate
(typecheck + typecheck:js + lint + build + `npm test`), commit + push to `main`.
`main` is **CI-gated auto-deploy** — push → GitHub Actions runs the full ladder →
**only if green** does it deploy to Vercel. Last commit at handoff: **`7cead81d`**.

---

## 1. Shipped this session — all on `main`, all validated green

| Item | What | Commit |
|---|---|---|
| C1 | org-member owner-escalation closed (RLS) | live migration `20260620000000` |
| C2 | pay-app figures immutable (UI + DB trigger + audit) | `83ed1811` |
| C3 | bulk-submittal approval locks linked sets | `2af2aa13`-era |
| C4 | email→submittal invalid status fixed | `2af2aa13`-era |
| I3 | org owners/admins READ **+ WRITE** all org projects | `e752f6fe` |
| I4 | revision-upload sheets carry `drawing_set_id` | `2af2aa13`-era |
| #1 | `user_metadata` can't override server role | `f74af871` |
| #2 | uploads fail-closed when org unresolved | `804e76df` |
| #3 | record numbering → atomic RPC (no dup numbers) | `699144e6` |
| #4 | llm-proxy model allowlist + maxTokens clamp | `27948b2a` ⚠️ **NEEDS DEPLOY** |
| #9 | deleted dead browser-direct LLM branch | `4c1d2e11` |
| #13 | atomic admin-gated `soft_delete_project` RPC | `7cead81d` |
| #16 | fixed stale deploy comments in `ci.yml` | `8e2194c7` |
| #17/18/19 | Sentry PII off · telemetry scrub · security headers | `76a7e286` |
| #20 | `resolveFileUrl` blocks untrusted external URLs | `c53791da` |
| #8 P1 | ESLint now covers TS/TSX + `src/lib`/`src/api` | `cca337cc` |
| #15 | **verified already-done** (commit `cc243318`) — no change | — |

**4 live migrations applied via Supabase MCP** (project `kjrwqagyeswwoxpjkcko`), each verified:
`20260620000000` org-members owner guard · `20260620010000` payapp line-immutability trigger ·
`20260620020000` org-admin write-parity helpers · `20260620030000` `soft_delete_project`.

---

## 2. ⚠️ PENDING — needs YOU

- **Deploy `llm-proxy`** to activate **#4** (+ **#10**):
  `supabase functions deploy llm-proxy --no-verify-jwt`
  Until deployed, the model allowlist + maxTokens clamp are committed-but-inactive.
  **#10** (bump client `EXPECTED_PROTOCOL_VERSION` 3→8 in `src/api/supabaseClient.ts`) should
  ride with that deploy — bumping before the live proxy is v8 would fail tool-use calls.
- **`ignoreCommand` decision (#16 config half):** `vercel.json` skips Vercel *preview* git-builds
  (cost) and does NOT affect the CI prebuilt deploy path. CLAUDE.md's "don't reintroduce an
  ignoreCommand — it errors the deploy" looks **stale**. Either keep it + update that CLAUDE.md
  line, or remove it to restore PR preview URLs. I left it untouched.

---

## 3. 🔴 Open — EDGE FUNCTIONS (I can write; **you must deploy + verify**)

Deno + supabase CLI are **not installed in this environment**, so edge functions can't be
`deno check`'d or deployed from here. Write the code, then `supabase functions deploy <fn>` + verify in-app.

- **#5** `llm-proxy/quota.ts` fails OPEN on a read error → fail-closed for expensive use-cases + a kill switch.
- **#6** `email-ingest` calls OpenAI directly (bypasses llm-proxy + the per-user quota) → route through llm-proxy or a shared quota helper; add per-project email-classify limits.
- **#7** `email-ingest` inbound attachments have no count/size cap, no filename sanitization, no extension allowlist (outbound `email-send` already has a 20 MB guard — mirror it).
- **#11** CORS `*` → production allowlist (`_shared/cors.ts`, `stripe-billing`, `llm-proxy` falls back to `*`).
- **#12** `stripe-billing` checkout success/cancel URLs trust the request `Origin` → validate against an allowlist.

---

## 4. 🟠 Open — FRONTEND/DB (I can fully ship next session)

- **#21** Upload validation: central `validateUpload` helper (extension allowlist + size cap **per workflow**), wire the upload call sites. Bigger but self-contained.
- **#14** Central `ProjectScopedRoute` wrapper (route-level project-scope guard).

---

## 5. ⚫ Staged — multi-session

- **#8 Phase 2 — strictNullChecks ratchet.** MEASURED: the base `tsconfig` *explicitly* sets
  `strictNullChecks:false`/`noImplicitAny:false`, so "strict on top" runs as **full** `strict`.
  Backlog = strictNullChecks **395 errs / 26 files** (≈85% in ~5 schedule/gantt screens) ∪
  noImplicitAny **334** ∪ 17 other-strict. Plan: add `typescript-strict-plugin`, mark the offender
  union with `// @ts-strict-ignore`, add a `tsc-strict` CI gate so the ~62 clean files + all NEW
  code are enforced. **CRITICAL:** build the opt-out list and confirm `tsc-strict` is green
  **locally before** wiring the CI gate — a failing gate blocks ALL production deploys.
- **#22** Large-file refactors — a campaign already exists (see the `refactor_campaign` memory).

---

## 6. Gotchas / facts for the next session

- **Verify before fixing.** Several audit findings were written against an older snapshot:
  **#15** already-done, **I5/I1** false on inspection. Check current code first (this saved fabricated changes).
- **Deploy topology:** `main` push → CI (lint/typecheck/test/build) → deploys only if green. A red
  push CANNOT reach prod (deploy job skipped). Edge functions deploy **separately** (CLI/MCP), not via the push.
- **RBAC (post-I3):** org owners/admins READ **and** WRITE every project in their org;
  `user_has_project_role_at_least` + `get_my_project_role` both carry the org override. Org `member` = explicit per-project grants only.
- **`soft_delete_project` RPC** owns the project-archive cascade now. Its 29-table array must stay in
  sync with the client's `SOFT_DELETE_TABLES ∩ PROJECT_SCOPED_TABLES` (`supabaseClient.ts`).
- **money = integer cents** app-wide (`src/lib/money.ts`); pay-app lines are now locked off-draft (trigger + UI).
- **Validation:** full `npm test` ≈ 2–4 min, **1579 tests**, green at every commit this session.
  The "Timeout terminating threads worker" line is a harmless teardown warning.

## 7. Suggested order from here
1. **You:** deploy `llm-proxy` (#4/#10) + decide `ignoreCommand` (#16).
2. **Next session:** #21 + #14 (frontend/DB, shippable end-to-end).
3. **Then:** #8 Phase 2 ratchet (focused — CI-gate care).
4. **Edge batch** #5/#6/#7/#11/#12 — I write, you deploy.
