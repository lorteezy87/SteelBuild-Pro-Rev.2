# Action plan IDs 14–17 — work log (2026-07-24)

## ID 14 — Verify SteelBuild-Pro sync reliability

**Status: Done (hardened this pass)**

| Sync path | Change |
|---|---|
| SharePoint / OneDrive Sync Now | No longer bumps `last_sync_at`; creates folders with `sync_enabled: false`; message via `sharepointSyncHonesty.ts` |
| Shipping list → production/canonical | Bridge/production failures now `toast.warning` + done-screen note (`summarizeShippingListCommit`) |
| Shipping ticket | UI copy: deliveries only (no production/canonical sync) |
| Data Exchange import | `bulkCreateWithFallback` returns `{ created, skipped }`; toast warns on skipped row creates |

Tests: `sharepointSyncHonesty.test.ts`, `summarizeShippingListCommit.test.ts`, updated `dataExchange.test.js`.

## ID 15 — Retest all corrected flows end to end

**Status: Done for automated coverage; interactive UAT still open (ID 102)**

- Harness: `npm run test:corrected-flows` → `scripts/retest-corrected-flows.mjs`
- Report: [`RETEST_CORRECTED_FLOWS.md`](./RETEST_CORRECTED_FLOWS.md)

## ID 16 — Find and fix remaining React hook-order violations

**Status: Done (verified clean)**

- AST + ESLint `react-hooks/rules-of-hooks` over `src/` — **0 violations**
- Guard: `npm run check:hooks`

## ID 17 — Review console errors and warnings across major pages

**Status: Done for actionable code; residual noise documented**

Sentry (`javascript-react`, last 14d) after #119 prod deploy:

| Issue | Assessment |
|---|---|
| CSP inline / vercel.live (9, R, Q, Z) | Report-Only noise / preview toolbar — not app defects |
| EnvValidationError (Y, N, P) | Staging env misconfig (URL/anon swap) — **owner ops** |
| model-elements timeout (11 / F) | Addressed by `fetchAllModelElements` paging + index (`20260724150000`); last events pre-date 2026-07-24 deploy — monitor |
| Deliveries RLS permission denied (15) | Anonymous/preview session — expected RLS |
| N+1 WorkPackages (8) | Performance follow-up, not console crash |
| Dashboard onError (12) | Handled react-query capture; triage separately if regresses |

Prior #119 fixes remain live: contacts FK embed, truncation noise, CSP open-meteo/wasm.
