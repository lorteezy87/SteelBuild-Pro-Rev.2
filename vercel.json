{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"preview\" ]; then exit 0; fi; npm ci --include=dev || exit 1; if [ ! -x node_modules/.bin/tsc ] || [ ! -x node_modules/.bin/eslint ]; then exit 1; fi; if npm run typecheck && npm run typecheck:js && npm run lint; then exit 1; else exit 0; fi",
  "headers": [
    {
      "source": "/wasm/(.*)\\.wasm",
      "headers": [
        { "key": "Content-Type", "value": "application/wasm" }
      ]
    },
    {
      "source": "(.*)\\.wasm",
      "headers": [
        { "key": "Content-Type", "value": "application/wasm" }
      ]
    }
  ],
  "rewrites": [
    { "source": "/wasm/:path*", "destination": "/wasm/:path*" },
    { "source": "/assets/:path*", "destination": "/assets/:path*" },
    { "source": "/((?!wasm/)(?!assets/).*)", "destination": "/index.html" }
  ]
}
