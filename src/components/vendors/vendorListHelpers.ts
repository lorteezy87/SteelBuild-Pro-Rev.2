/**
 * Pure status colors for VendorList.
 */

export const STATUS_COLORS: Record<string, string> = {
  Active: "var(--status-success)",
  Inactive: "var(--text-muted)",
  Probation: "var(--status-warning)",
  Suspended: "var(--status-error)",
};
