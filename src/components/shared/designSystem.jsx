// DEPRECATED: Use CSS variables from globals.css instead
// This file is maintained for reference only. All colors are now managed via:
// - globals.css (light/dark mode CSS variables)
// - Inline style props using var(--bg-surface), var(--text-primary), etc.

// CSS Variable Reference:
// ── Backgrounds ──
// var(--bg-base), var(--bg-surface), var(--bg-surface-secondary), var(--bg-input), var(--bg-sidebar)

// ── Text ──
// var(--text-primary), var(--text-secondary), var(--text-muted), var(--text-disabled)

// ── Borders & Dividers ──
// var(--border-default), var(--border-strong), var(--divider)

// ── Status Colors ──
// var(--status-success), var(--status-warning), var(--status-error), var(--status-info)

// ── Semantic Colors ──
// var(--accent), var(--accent-muted), var(--accent-border)
// var(--success-muted), var(--success-border)
// var(--warning-muted), var(--warning-border)
// var(--danger-muted), var(--danger-border)

// ── Shadows ──
// var(--shadow-sm), var(--shadow-md), var(--shadow-lg), var(--shadow-card)

// See globals.css for full token definitions and light/dark mode overrides.
export const LEGACY_COLORS = { /* Deprecated */ };