/**
 * ImpactBoard — thin re-export of the canonical Panel.
 *
 * Doc Control production UI uses `ImpactBoardPanel` via `DocControlPanel`.
 * This alias preserves the legacy export name for any lingering imports.
 */
export { ImpactBoardPanel as ImpactBoard } from "./ImpactBoardPanel";
